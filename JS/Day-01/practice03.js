// A good string is a string that starts with the letrer
// 'a' and length > 3 print good and bad dtring.
let str = 'apple';
if (str[0] == 'a' && str.length > 3) {
    console.log("Good String");
}
else { console.log('Not a Good string'); }